CREATE TABLE IF NOT EXISTS Releases (id BINARY(16) PRIMARY KEY, module_id INT UNSIGNED NOT NULL, release_version VARCHAR(20) NOT NULL, mod_version VARCHAR(20) NOT NULL, changelog TEXT NOT NULL, downloads INT DEFAULT 0 NOT NULL, created_at DATETIME(6) NOT NULL, updated_at DATETIME(6) NOT NULL, CONSTRAINT fk_Releases_module_id_id FOREIGN KEY (module_id) REFERENCES Modules(id) ON DELETE RESTRICT ON UPDATE RESTRICT)